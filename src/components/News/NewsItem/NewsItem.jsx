// 🎯 Импортируем React (обязательно для JSX)
import React from 'react';
// 🎯 Импортируем стили из CSS модуля
// styles - объект, содержащий все классы из NewsItem.module.css
import styles from './NewsItem.module.css';

// 🎯 СОЗДАЕМ КОМПОНЕНТ NewsItem (одна карточка новости)
// Компонент принимает пропсы (props) - данные которые передаются из родителя
// В фигурных скобках {} деструктурируем props для удобства
const NewsItem = ({ 
  
  title,        // Заголовок новости (строка)
  description,  // Краткое описание новости (строка)
  date,         // Дата публикации (строка в формате "2024-01-15")
  imageUrl,     // URL изображения новости (строка)
  readMoreUrl   // URL полной статьи (строка)
}) => {
  // 🎯 КОМПОНЕНТ ВОЗВРАЩАЕТ JSX - разметку одной новости
  // ВАЖНО: это один элемент списка (li), а не весь список
  return (
    // 🎯 li - элемент списка. Каждая новость - элемент списка NewsList
    // className={styles.newsItem} - применяем стиль из CSS модуля
    <li className={styles.newsItem}>
      
      {/* 🎯 ИЗОБРАЖЕНИЕ НОВОСТИ */}
      {/* Тег img для отображения картинки */}
      <img 
        className={styles.newsImage}     // Стиль для изображения
        src={imageUrl}                   // URL картинки из пропсов
        alt={title}                      // Альтернативный текст (для доступности)
        loading="lazy"                   // 🎯 Ленивая загрузка (оптимизация)
        // loading="lazy" - браузер загрузит изображение только когда 
        // оно появится в области видимости пользователя
      />
      
      {/* 🎯 КОНТЕЙНЕР ДЛЯ ТЕКСТОВОГО КОНТЕНТА */}
      <div className={styles.newsContent}>
        
        {/* 🎯 ЗАГОЛОВОК НОВОСТИ */}
        {/* h3 - заголовок третьего уровня (семантика HTML) */}
        <h3 className={styles.newsTitle}>
          {title}  {/* 🎯 Вставляем значение из пропса title */}
        </h3>
        
        {/* 🎯 ОПИСАНИЕ НОВОСТИ */}
        <p className={styles.newsDescription}>
          {description}  {/* 🎯 Вставляем значение из пропса description */}
        </p>
        
        {/* 🎯 ДАТА ПУБЛИКАЦИИ */}
        <p className={styles.newsDate}>
          {date}  {/* 🎯 Вставляем значение из пропса date */}
        </p>
        
        {/* 🎯 ССЫЛКА "READ MORE" */}
        {/* Тег a для создания гиперссылки */}
        <a 
          className={styles.newsLink}       // Стиль для ссылки
          href={readMoreUrl}                // URL из пропсов
          target="_blank"                   // 🎯 ТРЕБОВАНИЕ ТЗ: открывать в новой вкладке
          rel="noopener noreferrer"         // 🎯 БЕЗОПАСНОСТЬ: защита от уязвимостей
          // target="_blank" - ссылка откроется в новой вкладке/окне
          // rel="noopener noreferrer" - защищает от security vulnerabilities
          // когда используется target="_blank"
        >
          Read more  {/* 🎯 Текст ссылки (можно вынести в пропс если нужно) */}
        </a>
      </div>
    </li>
  );
};

// 🎯 ЭКСПОРТИРУЕМ КОМПОНЕНТ
// export default - делает компонент доступным для импорта в других файлах
export default NewsItem;